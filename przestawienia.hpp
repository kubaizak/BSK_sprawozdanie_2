#pragma once

#include <string>
#include <vector>

namespace Przestawienia{

    std::string encrypt(std::string, std::vector<int>);
    std::string decrypt(std::string, std::vector<int>);
    // std::string encrypt_old(std::string, vector<int>);

}